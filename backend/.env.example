# Supabase
SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# CORS origin allowed for the frontend (Vite dev server or production URL)
CORS_ORIGIN=http://localhost:5173

# Local dev server port
PORT=4000

# Demo password used by the seed script for all demo users
DEMO_PASSWORD=Barberia2026!

# Rate limiting for the API (requests per window per IP)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
